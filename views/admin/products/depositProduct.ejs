<% layout('/admin/template') -%>
<title>Deposit Product</title>
<script type="text/javascript">

function updateText(type) {
 var id = type+'Text';
 document.getElementById(id).value = document.getElementById(type).value;
}
</script>
<div class="page-heading">
  <h3>
    <%= __('Deposit Product') %>
  </h3>
  <ul class="breadcrumb">
    <li>
      <a href="/admin">Dashboard</a>
    </li>
    <li>
      <a href="/admin/productList"><%= __('Products List') %></a>
    </li>
    <li class="active"> <%= __('Deposit Product') %> </li>
  </ul>
</div>
<div class="wrapper">
  <div class="row">
    <div class="col-lg-12">
      <section class="panel">
        <header class="panel-heading">
          <%= __('Deposit Product') %>
        </header>
        <div class="panel-body">
          <% if (msg.length > 0) { %>
            <div class="alert alert-success alert-dismissible">
              <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>

              <%= msg %>

            </div>
            <% } %>
            <% if (error.length > 0) { %>
              <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                  <span aria-hidden="true">&times;</span>
                </button>

                <%= error %>

              </div>
              <% } %>
              <form method="post">
                <div class="form-group">
                  <label for="name"><%= __('Customers') %></label>
                  <select class="form-control" data-style="btn-info" id="cus" name="cus" onchange="updateText('name')">
                    <optgroup label="------------------">
                      <% var select = 'a' %>
                      <% for (var i = 0; i < customers.length; i++) { %>

                        <option name="cus1" value="<%= customers[i]._id %>"><%= customers[i].name %> - <%= customers[i].phone %></option>

                      <% } %>
                    </optgroup>
                  </select>
                </div>
                <div class="form-group">
                  <label for="depositPrice"><%= __('Deposit Price') %></label>
                  <input type="number" class="form-control" id="depositPrice" name="depositPrice" min="50000000" placeholder="" required="true">
                </div>
                <div class="form-group">
                  <label for="code"><%= __('Project') %></label>
                  <select class="form-control" data-style="btn-info" disabled id="name" name="name" onchange="updateText('name')">
                    <optgroup label="------------------">
                      <% var select = 'a' %>
                      <% for (var i = 0; i < projects.length; i++) { %>
                        <% if (product.project.name == projects[i].name) { %>
                      <option name="pro" selected value="<%= projects[i].name %>"><%= projects[i].name %></option>
                      <% } else { %>
                        <option name="pro" value="<%= projects[i].name %>"><%= projects[i].name %></option>
                        <% } %>
                      <% } %>
                    </optgroup>
                  </select>
                </div>
                <div class="form-group">
                  <label for="code"><%= __('Apt. Code') %></label>
                  <input type="text" class="form-control" id="code" name="code" disabled value="<%= product.code %>" placeholder="C03.12-09, P05.19-5,..." required="true">
                </div>
                <div class="form-group">
                  <label for="rooms"><%= __('Rooms') %></label>
                  <input type="number" class="form-control" id="rooms" name="rooms" disabled value="<%= product.rooms %>" placeholder="1, 2, 3, 4,..." required="true" step="0.01">
                </div>
                <div class="form-group">
                  <label for="area"><%= __('Area') %> (m<sup>2</sup>):</label>
                  <input type="number" class="form-control" id="area" name="area" disabled value="<%= product.area %>" placeholder="100, 105.5, 89.7,..." step="0.01" required="true">
                </div>
                <div class="form-group">
                  <label for="sellPrice"><%= __('Sell Price') %></label>
                  <input type="number" class="form-control" id="sellPrice" disabled value="<%= product.sellPrice %>" name="sellPrice" placeholder="3.4, 4.5,..." step="0.01" required="true">
                </div>
                <div class="form-group">
                  <label for="rentPrice"><%= __('Rent Price') %></label>
                  <input type="number" class="form-control" id="rentPrice" disabled value="<%= product.rentPrice %>" name="rentPrice" placeholder="1200, 1500,..." step="0.01" required="true">
                </div>
                <div class="form-group">
                <button type="submit" class="btn btn-primary"><%= __('Deposit') %></button>
              </div>
              </form>

            </div>
          </section>
        </div>
      </div>
    </div>
